# datakraften.no

Developer Tooling — fra utvikler til utvikler.

## WSL Developer Bootstrapper

```bash
curl -fsSL https://datakraften.no/wsl/debian.sh | bash
```

## Struktur

```
bootstrap/
└── wsl/
    └── debian.sh   ← WSL Debian bootstrapper
```

## Lokal utvikling

Åpne `index.html` i en nettleser.
